# v0.5 — Integrated Development Infrastructure — Release Readiness

Status: **v0.5.0 RELEASED — R3 IMMUTABLE TAG VERIFIED**

This is the candidate release-readiness ledger for the canonical v0.5 milestone. It follows the accepted planning contract in `docs/v0.5-release-readiness-plan.md` and introduces no numbered architecture phase or runtime authority.

The R2 base is exact accepted R1 readiness merge:

```text
76c1eeb32e996df11fe7e946e81e10d8b64b7afb
```

R2 set package version `0.5.0` after the accepted readiness audit. Final candidate CI/review/merge and R3 annotated-tag proof are complete; `v0.5.0` is released at immutable commit `8ac46ee5f14654187469e79b021dbbd83992270b`.

## Readiness conclusion

The canonical roadmap's v0.5 target capabilities are implemented on current mainline. The detailed mapping is in `docs/v0.5-acceptance-matrix.md`.

No target uncovered a missing production-runtime feature that requires an invented Phase 48. Therefore release readiness may proceed as release/documentation/packaging work under the frozen no-authority-widening contract.

The current payload also includes accepted post-v0.1 Phases 31–47, providing integrated production planning/dispatch/Manager/Goal-bootstrap/simulation mechanics beyond the original milestone target list. Their inclusion does not create automatic Task completion, adoption/signing, model self-verification, background scheduling, merge, or release authority.

## Current candidate identity

At the R2 final candidate:

```text
package:        origin-forge
version:        0.5.0
license:        Apache License 2.0
Python floor:   >=3.12
build backend:  setuptools.build_meta
build floor:    setuptools>=68
```

Installed scripts remain exactly:

```text
origin-forge          durable control-plane/operator surface
origin-forge-attempt  one bounded coding attempt
origin-forge-cockpit  read-only local inspection
```

Manager and Goal-bootstrap remain `origin-forge` subcommands. Deterministic simulation production execution is reachable only through already-governed Manager dispatch; there is no direct simulation mutation entrypoint.

## Architecture readiness — SATISFIED

The v0.5 milestone targets are covered by accepted implementation:

- durable long-running work — Phase 1 plus bounded restart/recovery/Manager production state;
- local coding models — Phase 2 plus Phase-14 governed local model/runtime/resource scheduling;
- governed Skills + evaluation — Phases 9 and 12;
- structural/LSP intelligence — Phases 10 and 11;
- progressive tool discovery — Phase 13;
- model/resource scheduling — Phase 14;
- offline memory consolidation and audited improvement proposals — Phases 15, 26, 27 and 29;
- specialist review — Phase 16;
- project graph + Design Bible — Phase 17;
- cryptographic provenance — Phase 18;
- basic 2D production — Phase 19.

No release-only code path is required to make those statements true.

## Integrated post-v0.1 payload — ACCEPTED

Phases 31–47 are post-v0.1 development and are included in the current v0.5 candidate payload unless deliberately removed before R2. They provide:

- governed planning / dependency graph;
- capability routing and bounded WorkOrders;
- exact dispatch binding/currentness;
- durable claims/execution ownership;
- single-shot reviewed owner invocation;
- bounded Manager admission, preparation, recovery and fixed advancement;
- explicit local Manager operator commands;
- durable Goal bootstrap with explicit start/recover separation;
- deterministic simulation dispatch through the same no-replay execution law.

The Phase-47 closure is merged as `a65a3e78ae3c6fb762cbe5f46a510adf22804c49` after exact-head normal run `31913018996` / #1355 passed Python 3.12 and 3.13.

## Packaging readiness — SATISFIED FOR R2 CANDIDATE

Existing packaging regression already proves:

- package name and development version are exact;
- Apache-2.0 `LICENSE` is present and validated;
- Python/build-system floors are explicit;
- script mappings are exactly the existing three;
- attempt parser exposes one bounded explicit/automatic-context attempt;
- attempt help/uninitialized/config-only/empty-database/cross-project/active-journal cases do not silently initialize/migrate project state;
- cockpit parser remains only `snapshot|serve`;
- cockpit help/uninitialized inspection does not create project state.

Phase-specific regressions separately prove Manager and Goal-bootstrap remain subcommands and do not become extra package scripts/background services. No R1 packaging-test mutation is required merely to restate already-covered behavior.

## Operator readiness — SATISFIED / FROZEN FOR R2

The living `docs/operator-guide.md` is synchronized through Phase 47 and documents:

- explicit project initialization;
- one bounded coding attempt;
- read-only cockpit;
- explicit `manager status|advance`;
- explicit `goal bootstrap status|start|recover GOAL-ID`;
- code-only Goal-bootstrap authority;
- governed deterministic simulation execution only through existing Manager dispatch;
- no replay after uncertain STARTED execution;
- no direct simulation mutation command.

R1 froze this accepted surface into `docs/v0.5-operator-guide.md`. R2 changes only release identity wording to the `0.5.0` final candidate; it does not widen the command/authority surface. Tagging remains downstream evidence rather than an operator capability.

## Documentation and release-identity synchronization

R1 corrected README/CHANGELOG release-maintenance drift through Phase 47 and added the v0.5 readiness, acceptance, and operator records while retaining `0.2.0.dev0`.

R2 is allowed to mutate only:

- `pyproject.toml` version `0.2.0.dev0 → 0.5.0`;
- the matching existing packaging regression assertion;
- README/CHANGELOG/v0.5 readiness/acceptance/operator wording needed to identify the final `0.5.0` candidate.

R2 must not change package script mappings, production source, schema, protected config, runtime/model/resource/Manager/Goal-bootstrap/simulation semantics, trust roots, historical v0.1 records/tag, or workflow definitions in the final tree.
## Authority exclusions preserved

Release readiness adds no:

- automatic Task verification/completion;
- automatic Artifact adoption/signing;
- model self-verification;
- automatic Dream/Workshop/training activation;
- checkpoint/model activation;
- unrestricted shell/filesystem/process/model/tool authority;
- cockpit mutation or remote hosting;
- background Manager/Goal-bootstrap scheduler, watcher, poller, daemon, or queue drain;
- generic owner/plugin dispatch;
- automatic simulation replay/tuning/Task terminalization;
- automatic merge, push, release, deployment, or tag creation inside Origin Forge.

## Historical v0.1 boundary

v0.1.0 remains immutable historical evidence:

- release commit: `fbc6764b3b5e71cb5f5a223f09c82189e7326c1d`;
- final candidate normal run: `31478577762`;
- tag `v0.1.0` points to that exact release commit;
- archival readiness/acceptance/operator records remain `docs/v0.1-*`.

v0.5 documentation must not rewrite those files to describe later capabilities.

## Accepted R0 planning evidence

- R0 planning head: `acd44522cebec5f109b08fa894d67557e7d5faab`;
- normal workflow: `31913373190` / #1357;
- Python 3.12: PASS;
- Python 3.13: PASS;
- planning diff: exactly `docs/v0.5-release-readiness-plan.md`;
- clean review/thread state;
- SHA-guarded merge: `4d52471c4cb1040f6678d45d005f650b8114a4fc`.

## Accepted R0 / R1 evidence

- R0 planning head `acd44522cebec5f109b08fa894d67557e7d5faab` passed normal run `31913373190` / #1357 on Python 3.12 and 3.13 and SHA-guarded merged as `4d52471c4cb1040f6678d45d005f650b8114a4fc`.
- R1 readiness head `b7aa954b77667b22a2a757dc4e5512a1a03bced9` passed normal run `31913775339` / #1360 on Python 3.12 and 3.13, had clean review/thread state and exactly five documentation files, and SHA-guarded merged as `76c1eeb32e996df11fe7e946e81e10d8b64b7afb`.

## R2 exit condition

The final R2 diff must be limited to the `0.5.0` package/test identity pair plus release-candidate wording in the already-reviewed release documents. Production/source/schema/config/workflow/tag state must remain unchanged.

The exact final R2 head must:

1. pass the normal Python 3.12 and 3.13 matrix with `ResourceWarning` treated as error;
2. retain exactly the existing three package scripts and Apache-2.0/Python/build metadata;
3. have clean review submissions and unresolved review threads;
4. be SHA-guarded merged.

Only that exact tested merge may receive the annotated `v0.5.0` tag.

## R3 immutable release evidence — COMPLETE

The final release proof is:

- final R2 candidate head: `818de3348834709b58d8117d45539e1a80be1298`;
- authoritative normal workflow: `31914257104` / #1362;
- Python 3.12: PASS;
- Python 3.13: PASS;
- R2 diff: exactly seven release files — five release documents, `pyproject.toml`, and `tests/test_release_packaging.py`;
- production source/schema/config/workflows: unchanged in the final R2 tree;
- review submissions / unresolved threads: none;
- SHA-guarded R2 merge / release commit: `8ac46ee5f14654187469e79b021dbbd83992270b`;
- R3 tag helper run: `31914507703` — SUCCESS;
- annotated tag: `v0.5.0`;
- annotated tag object: `b45c1ef4cbb5b219d165331dff96ffcfa10cf609`;
- tag object target: exact release commit `8ac46ee5f14654187469e79b021dbbd83992270b`;
- tag message: `Origin Forge v0.5.0`;
- tag verification metadata: unsigned; no cryptographic Git-tag signature claim is made.

The `v0.5.0` ref is the immutable release identity. Any later README/readiness/roadmap maintenance is post-release documentation and must not move or replace that tag.
